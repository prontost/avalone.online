[project]
name = "avalone-core"
version = "0.1.0"
description = "Shared core for Avalone platform modules."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["avalone_core"]
